<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.android.bookmyvisitzmedios">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:replace="android:icon">
        <activity android:name=".Category_detail_activity"></activity>
        <activity android:name=".LoginSignupUser.ResetPasswordActivity" />
        <activity android:name=".LoginSignupUser.LoginActivity" />
        <activity android:name=".Homescreen.HomeScreen" />
        <activity android:name=".LoginSignupUser.SignupActivity" />
        <activity android:name=".OtpLogin.OtpLogin" />
        <activity android:name=".FirstScreen.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>